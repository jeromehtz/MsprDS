[pytest]
# Rend les modules de l'API importables (from main import app, from database import ...)
pythonpath = .
testpaths = tests
addopts = -v --tb=short
markers =
    unit: tests unitaires de composants isolés
    integration: tests d'intégration backend / base de données
